# Force pip, wheel, and setuptools for binary-only installation
pip>=25.2
wheel
setuptools>=68

# Core dependencies
Flask==3.0.2
Flask-CORS==4.0.0
requests==2.31.0
urllib3==2.0.7
itsdangerous==2.1.2
python-dotenv==1.0.0

# Production server
gunicorn==21.2.0

# Database
# supabase==2.7.4  # Removed due to pydantic/Rust compilation issues
psycopg2-binary==2.9.9

# Scheduling
APScheduler>=3.10,<4.0

# Utilities
python-dateutil==2.8.2

# Async support
aiohttp>=3.8,<4.0

# HTTP client compatibility
httpx>=0.24,<0.25

# Type hints compatibility
typing-extensions>=4.7,<5

# Cryptography for secure operations (safe range with wheels for Python 3.11)
cryptography>=42,<46